// Server entrypoint for the annotate plugin (TUI-only in practice).
// Required so OpenCode gives the plugin a proper identity on the server side.

export default {
  id: "annotate",
  setup() {},
}
