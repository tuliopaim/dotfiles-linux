{
    "$schema": "https://opencode.ai/config.json",
    "autoupdate": true,
    "skills": {
        "paths": [
            "~/dotfiles/skills"
        ]
    },
    "lsp": false,
    "permissions": [
        { "action": "*", "resource": "*", "effect": "allow" },
        { "action": "read", "resource": "~/.ssh", "effect": "deny" },
        { "action": "read", "resource": "~/.ssh/*", "effect": "deny" },
        { "action": "shell", "resource": "*~/.ssh*", "effect": "deny" },
        { "action": "shell", "resource": "*$HOME/.ssh*", "effect": "deny" }
    ],
    "mcp": {
        "chrome-devtools": {
            "type": "local",
            "enabled": false,
            "command": [
                "npx",
                "-y",
                "chrome-devtools-mcp@latest"
            ]
        }
    },
    "agents": {
        "review": {
            "description": "Delegate focused, read-only code review to a high-reasoning model. Call this after a long or high-risk implementation finishes: an independent fresh-context review is materially useful. Give it the exact scope (working tree, commit/range, or files) plus intended behavior. Do not invoke it automatically for small, low-risk changes, and use it at most once per change unless new code is added after the review.",
            "mode": "all",
            "model": "openai/gpt-5.6-sol#high",
            "color": "#ff6b6b",
            "system": "You are a read-only code reviewer. Review the delegated change or scope; do not edit files.\n\nReturn only this compact handoff:\n## Findings\nFor each real issue, ordered by severity:\n### [P0-P3] Short title\n- Evidence: path:line\n- Impact: what breaks and under which conditions\n- Fix: smallest correct change\n\nIf there are no findings, write \"No findings.\"\n\n## Validation gaps\n- Important behavior you could not verify, or \"None\".\n\n## Verdict\nOne sentence stating whether the change is safe to merge.\n\nRules:\n- Prioritize correctness, security, data loss, regressions, and missing validation.\n- Review the actual diff and trace affected callers when relevant.\n- Do not report style preferences, speculative concerns, or pre-existing issues unrelated to the change.\n- Do not modify files or run shell commands.\n- Prefer exact file paths and line numbers over prose.\n- Stay under 1,200 words.",
            "permissions": [
                { "action": "edit", "resource": "*", "effect": "deny" },
                { "action": "shell", "resource": "*", "effect": "deny" }
            ]
        },
        "commit": {
            "description": "Create intentional git commits with the specialized commit agent",
            "mode": "all",
            "model": "opencode-go/deepseek-v4-flash#high",
            "color": "#4caf50",
            "system": "You are a specialized git commit agent sharing the current working tree. Use the commit-work skill and follow it exactly. Inspect all changes before staging, keep unrelated work uncommitted, never expose secrets, never amend or force push, and report each created commit's SHA and message.",
            "permissions": [
                { "action": "edit", "resource": "*", "effect": "deny" },
                { "action": "shell", "resource": "git push*", "effect": "deny" },
                { "action": "shell", "resource": "git rebase*", "effect": "deny" },
                { "action": "shell", "resource": "git reset --hard*", "effect": "deny" },
                { "action": "shell", "resource": "git clean*", "effect": "deny" },
                { "action": "shell", "resource": "git commit --amend*", "effect": "deny" }
            ]
        }
    }
}
