---
description: Create intentional commits with the specialized commit agent
agent: commit
---

$ARGUMENTS

Analyze all completed work and create the appropriate commit or commits. Separate unrelated changes into one or more coherent commits, ignore docs or unrelated files unless they are part of the requested work, stage only intended files, and create concise conventional commits. Never commit secrets, never force push, and report exactly what you committed.
