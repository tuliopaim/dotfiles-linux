---
description: Analyze completed work and create conventional commits
---

Use the commit-work skill. $ARGUMENTS

Analyze the completed work, separate unrelated changes into one or more coherent commits when needed, ignore docs or unrelated files unless they are part of the requested work, stage only intended files, and create concise conventional commits. Never commit secrets, never force push, and report exactly what you committed.
